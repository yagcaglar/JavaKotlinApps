package com.yagmur.oop;

public interface Decor {
    //cannot create object directly
    //methods must be overwritten, cannot have body
    //properties are final and should be initialized

    String hi = "hello";
    public void info();
}
